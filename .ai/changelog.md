# Changelog (agent OS)
- 2026-09-26: v1.1.0 release — commits the full 2026-08-26 working set that had been sitting uncommitted: `va` CLI, Mission Control (`app/desktop/`), project templates catalog, model auto-allocation, CI, and 20 new tests. Housekeeping: `app/opencode-dev/` (~2.9 GB vendored upstream study fork) added to `.gitignore` so it can never reach the published template; removed six one-off August migration scripts/screenshot from `app/` root. Verified before release: 91/91 tests green, E2E bootstrap from a clean clone produces a working workspace with the guardrail hook actively blocking forbidden files, npm tarball ships the parser an installed `va` needs, Mission Control boots and renders live `.ai/` state.
- 2026-08-25: T-401 closed — desktop dev launch fixed via `scripts/dev.ts` env sanitizer (see KB-001, ADR-008).
- 2026-08-25: Template hardening v1.0.11 — pre-commit Windows path-separator fix + `plans/` & `graphify-out/` allowlist (+ regression tests, CI on ubuntu/windows); CLI hardened (TTY guard, existing-dir guard, failure cleanup, `--version`, neutralized bootstrapped manifest); bootstrap resets `.ai/` state to pristine defaults (`bin/reset-template-state.js`); npm tarball scoped via `files`; docs contradictions resolved (paste phrases, stale model names, boundary laws).
- 2026-08-26: Mission Control desktop app added at app/desktop/ (ADR-009): live .ai/ dashboard, contract-violation alerts, copy-paste phrase actions, env-sanitized launcher; 10 parser tests green.
- 2026-08-26: Deep-scan bug fixes — parser CRLF normalization (Windows autocrlf checkouts broke status/phrase detection), start.js now forwards CLI args (--root), pre-commit allows .github/, CLI spawns npm.cmd on Windows + --help flag. 5 regression tests added; 45/45 green.
- 2026-08-26: Deep scan #2 — contract validation extracted to lib/parser.js (validateContract) now also enforcing none-with-open-rows, dependency-done, and duplicate-id rules; fixed double IPC handler registration on window re-activate; pre-commit emits raw UTF-8 paths (core.quotepath=false) for Unicode filenames; parseStateFile collects duplicate keys as arrays instead of overwriting. 9 new tests; 53/53 green.
- 2026-08-26: Deep scan #3 + feature batch — pre-commit permits staged deletions of stray files (cleanup unblockable); fs.watch debounce (150ms) stops Windows event storms. NEW: `va` status CLI (queue/active-ticket/phrase display, --copy to clipboard, --summary wave report, exit codes for scripting, registered as npm bin); Mission Control relay timeline (per-step ✓ VERIFIED / ▶ RESUME HERE badges from relay evidence). 4 new tests; 57/57 green.
- 2026-08-26: Mission Control motion layer — staggered section entrances, change-flash highlights on live queue/status updates (diff-based, only changed cells animate), breathing glow + light sweep on the active paste-phrase strip, copy-button pop feedback, alert pulse on contract violations, hover transitions; full prefers-reduced-motion support. Verified via in-page DOM inspection against an in_progress fixture (badges/resume/stagger/active-state all correct).
- 2026-08-26: Research-driven deep scan (Roo Code docs, Electron security guide, git githooks docs) — removed manual `source: project` from .roomodes + docs template (Roo manages that field; manual set is against spec); pre-commit hook now stored executable (100755) in the index so macOS/Linux clones don't silently lose the guardrail (+ regression test); package `files` whitelist extended to app/desktop/lib/ so installed `va` finds its parser (verified end-to-end via npm pack → install → run); README fixed `npx va` (would fetch an unrelated registry package) to proper install instructions; Mission Control gained will-navigate + window-open lockdown per Electron guide #13/#17; CI gained a pack-install smoke test that would have caught the packaging bug; engines field added (node >=18); .ai/README_AI.md tooling section rewritten and pinned by doc-consistency tests; .roorules first-feature mandate slimmed since bootstrap already does the rename. 61/61 tests green.
- 2026-08-26: Fifth mode added — 5-hermes "📣 Docs & Release Herald": documentation/release-notes ownership with doc-only fileRegex edit restriction, evidence-first rules, human-approved publishing (ADR-010). Propagated across .roorules constitution, README modes table, USER_GUIDE, docs switch_mode template, .ai/README_AI.md ownership list, CLI help, Mission Control footer. Contract pinned by tests (slug roster, charset per Roo spec, doc-only groups).
- 2026-08-26: Deep scan #4 (Hermes-focused) — closed guardrail hole: Hermes' fileRegex contained unanchored `.githooks/` and `README`/`CHANGELOG` substrings, granting the docs mode write access to .githooks/pre-commit and any `src/*README*` code file; replaced with anchored allowlist regex (`^(\.ai/changelog\.md|[^/]*\.md$|docs/.+|plans/.+)$`, 12-case behavioral test + permanent regression test). Added CHANGELOG.md/CONTRIBUTING.md to pre-commit root allowlist so Hermes-compliant commits pass the repo's own hook. Resolved changelog ownership conflict: Boss owns structure, Hermes appends dated entries only (rule 1+4 of README_AI).
- 2026-08-26: Project Templates catalog (ADR-011) — standard starters under templates/ (planner, todo, website): dependency-free working apps + pre-filled .ai/project_overview+config so Boss starts warm. CLI: --template/-t flag with catalog-driven validation and .ai seeding; file:// URLs allowed in VIBE_TEMPLATE_REPO for local testing. Fixed during build: flag parsing had broken --help/--version (positional walk now flag-aware); templates/ added to npm files whitelist and pre-commit allowlist. E2E verified via local fixture repo (planner + todo); 7 new tests; 69/69 green.
- 2026-08-26: Deep scan #5 (templates-focused) — three bootstrap-order/state bugs fixed: (1) template .ai seeds overwrote the renamed project_config AFTER placeholder fill, leaving CHANGE_ME_PROJECT_NAME in final workspaces → applyProjectTemplate now runs before initMetadata (empirically verified: name filled); (2) unknown --template name exited mid-flow after clone+rename, bypassing failure cleanup and leaking a partial project → validation moved before any side effects; (3) bare --template silently bootstrapped with no template → loud usage error. 3 new regression tests; 72/72 green.
- 2026-08-26: Deep scan #6 — relay-evidence matching had two bugs shared by Mission Control timeline and va: "Step 12" substring-matched "Step 1" (prefix collision marked 9 false-verified steps) and the PASS regex tested the whole notes blob so one success leaked to every step. Fixed via new shared verifiedSteps() in lib/parser.js: per-note word-boundary step mention AND per-note success evidence required together. Main precomputes; renderer and va consume. 5 semantic tests + live collision fixture verified (0/3 where old code showed 1/3); 77/77 green.
- 2026-08-26: Model Combo + Auto-Allocation (ADR-012) — .ai/model_knowledge.json (curated capability classes: reasoner/coder/flagship/workhorse/fast; per-mode primary+fallback requirements; benchmark refresh sources); bin/allocate-models.js (two-pass allocator: locks first, primary-class claims resolve scarcity to the mode that needs it — lone reasoner lands on Debug, not Boss; heuristic fallback for unknown names). va --models renders the assignment table from .ai/model_combo.txt. Bugs caught during build: alias-precedence misclassification (deepseek-r1 → workhorse, root cause: scoring by max key length of partially-matched entries instead of longest MATCHING key) and fallback-chain starvation. 8 tests; 80/80 green.
- 2026-08-26: Long-run batch — classifier hardened against stray short fragments ("r" no longer confidently matched via reverse containment); va --models supports inline comments + full-line comments; NEW: va --doctor (workspace health check: state files, contract consistency, CHANGE_ME detection, model-combo coverage); Mission Control gained a MODEL ALLOCATION side panel (hidden when no combo, live-updating); .ai/model_combo.example.txt ships as discoverable starter; 3 new tests; 88/88 green.
- 2026-08-26: Long-run batch #2 — clipboard fallback chain (wl-copy → xclip → xsel) for Wayland-only Linux; .roorules `?` SOS may consult va for deterministic answers; USER_GUIDE gained manual-setup (no-CLI clone path) and model-combo/doctor sections; verified resetTemplateState never clobbers template-seeded overviews (only-overwrite-existing design confirmed by probe); verifiedSteps edge probes pass (phantom step notes, step-0, unicode). 88/88 green.
- 2026-08-26: Long-run batch #3 — global rules flow updated to the 5-mode pipeline (was missing Chat/Hermes); va --json added (machine-readable status for scripts; tested); README command audit passed (every advertised invocation verified live); Mission Control full-boot regression with all session changes passed (window confirmed via full-title enumeration — earlier MISSING readings were visibility-flag quirks). 91/91 green.
- 2026-08-26: README gained a Token Usage section — the three token sinks (re-orientation, chat bloat, retry storms) mapped to the workspace's defenses, six practical budget rules, and a relative-cost table steering zero-token status checks to Mission Control/va.
- 2026-08-26: Rune-inspired discipline adoption (studied github.com/Rune-kit/rune) — (1) Loop-Prevention Law: 2 failed attempts → stop, record, escalate (global constitution law + Code rule 9 + Debug rule 5; VA's own troubleshooting warned about infinite loops but no rule prevented them); (2) Safety Net First for Debug: verbatim failing-output capture into known_bugs.md before any surgery; (3) Completion Gate in ticket contract: done requires every step exit-code-0 AND state still consistent. Rejected with reasons: mesh signal graph (over-engineered — disk state IS the bus), multi-platform compiler (Roo-specific identity). Pinned by 3 tests; 91/91 green.

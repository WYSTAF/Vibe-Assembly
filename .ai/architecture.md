# 🏛️ Dynamic System Architecture
Technical Topology: [Describe or display how services, frontends, backends, and storage modules connect]
Data Relations & API Contracts: [Boss logs schema fields, endpoints, or data state designs here before assigning coding tasks]

# System Architecture
All functional extension views (popup), background workflows, content scripts, and python bridges are contained strictly within the `app/` directory. The root of the workspace holds the project memory and agent configuration.